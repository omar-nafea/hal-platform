# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Ignore Laravel backend files when working in the React `main` branch
# These paths point to the `hal-platform` backend directory so the React
# branch doesn't show the Laravel runtime files as untracked.
/hal-platform/.env
/hal-platform/vendor/
/hal-platform/storage/
/hal-platform/bootstrap/cache/
